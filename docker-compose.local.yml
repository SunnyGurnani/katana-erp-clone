version: "3.9"

# Local development stack — run with: docker compose -f docker-compose.local.yml up --build
# Access the app at http://localhost

services:
  mysql:
    image: mysql:8
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: localdev123
      MYSQL_USER: forge
      MYSQL_PASSWORD: localdev123
      MYSQL_DATABASE: forgeerp
    volumes:
      - mysqldata_local:/var/lib/mysql
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "forge", "-plocaldev123"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - internal

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin123
    volumes:
      - minio_local:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - internal

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: mysql://forge:localdev123@mysql:3306/forgeerp
      SECRET_KEY: local-dev-secret-key-minimum-32-chars-long-ok
      ACCESS_TOKEN_EXPIRE_MINUTES: "60"
      REFRESH_TOKEN_EXPIRE_DAYS: "30"
      MINIO_ENDPOINT: minio
      MINIO_PORT: "9000"
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin123
      MINIO_BUCKET: forgeerp
      MINIO_USE_SSL: "false"
      MINIO_PUBLIC_URL: http://localhost/files
      NODE_ENV: production
      PORT: "4000"
      ALLOWED_ORIGINS: http://localhost,http://localhost:3000
      PUBLIC_API_URL: http://localhost
    depends_on:
      mysql:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - internal

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://localhost
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: http://localhost
    depends_on:
      - api
    networks:
      - internal

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
      - web
      - minio
    networks:
      - internal

volumes:
  mysqldata_local:
  minio_local:

networks:
  internal:
    driver: bridge
